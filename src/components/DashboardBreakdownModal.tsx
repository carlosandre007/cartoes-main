import React from 'react';
import { X } from 'lucide-react';

export interface DashboardBreakdownRow { id: string; description: string; detail: string; value: number; }
export interface DashboardBreakdown { title: string; description: string; total: number; rows: DashboardBreakdownRow[]; }
const brl = (value: number) => value.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' });

export const DashboardBreakdownModal: React.FC<{ data: DashboardBreakdown; onClose: () => void }> = ({ data, onClose }) => <div className="fixed inset-0 z-50 bg-black/80 flex items-center justify-center p-4"><div role="dialog" aria-modal="true" className="w-full max-w-3xl max-h-[90vh] overflow-y-auto rounded-2xl border border-amber-500/30 bg-zinc-950 p-5 space-y-4"><div className="flex justify-between gap-3"><div><h2 className="font-black">{data.title}</h2><p className="text-xs text-zinc-400 mt-1">{data.description}</p></div><button onClick={onClose} aria-label="Fechar"><X className="w-5 h-5" /></button></div><div className="p-3 rounded-xl bg-amber-500/10 border border-amber-500/20 flex justify-between"><span className="text-xs text-zinc-300">Total</span><b className="font-mono text-amber-300">{brl(data.total)}</b></div><div className="space-y-2">{data.rows.length ? data.rows.map((row) => <div key={row.id} className="p-3 rounded-xl bg-zinc-900 border border-zinc-800 flex justify-between gap-3"><div className="min-w-0"><b className="text-xs block truncate">{row.description}</b><span className="text-[10px] text-zinc-500">{row.detail}</span></div><b className="text-xs font-mono text-zinc-200 shrink-0">{brl(row.value)}</b></div>) : <p className="text-xs text-zinc-500 text-center py-6">Nenhum registro compõe este indicador.</p>}</div><p className="text-[10px] text-zinc-500">A soma considera todos os registros carregados, independentemente da quantidade exibida nesta lista.</p></div></div>;
